'use strict';

describe('myApp.driverDetails module', function() {

  beforeEach(module('myApp.driverDetails'));

  describe('driverDetails controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var driverDetailsCtrl = $controller('DriverDetailsCtrl');
      expect(driverDetailsCtrl).toBeDefined();
    }));

  });
});